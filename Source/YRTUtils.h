#ifndef YRTUtilsH
#define YRTUtilsH

#define title           "YAC Ray Tracer"

#define dot(u, v)       ((u).X * (v).X + (u).Y * (v).Y + (u).Z * (v).Z)
#define sqr(u)          ((u)*(u))
#define PI              3.1415926536
#define EPSILON         1e-10
#define MAX_FLOAT       3.4E38

#endif
